---
title: "Example project"
description: "Placeholder entry — replace this with a real project, or delete it once you've added your own."
stack: ["Replace me"]
repoUrl: "https://github.com/janino163"
date: 2026-07-12
---

Add one Markdown file per project in `src/content/projects/`. Frontmatter
fields: `title`, `description`, `stack` (array of tags), optional `repoUrl`
and `liveUrl`, and an optional `date` used for sorting. The body of the file
isn't currently rendered on the projects page — it's just cards linking out —
but it's there if a per-project detail page gets added later.
